# MVP 需求文档：Image Background Remover

## 1. 产品概述

| 项目 | 说明 |
|------|------|
| 产品名称 | Image Background Remover |
| 产品定位 | 轻量级在线图片去背景工具 |
| 核心价值 | 无需安装软件，浏览器直接上传去除图片背景 |
| 目标用户 | 设计师、电商运营、内容创作者 |

---

## 2. 核心功能 (MVP)

### 2.1 功能清单

| 优先级 | 功能 | 描述 |
|:------:|------|------|
| P0 | 图片上传 | 支持拖拽上传 + 点击选择，单文件 |
| P0 | 一键去背景 | 调用 Remove.bg API 处理图片 |
| P0 | 结果预览 | 展示去背景后的图片（透明背景用棋盘格显示） |
| P0 | 图片下载 | 下载为 PNG 格式（保留透明通道） |
| P1 | 加载状态 | 上传/处理中显示进度指示 |
| P1 | 错误提示 | API 失败、网络错误等友好提示 |

### 2.2 用户流程

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  上传图片  │ → │  发送API  │ → │ 处理中   │ → │ 预览下载 │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
      ↑                                         │
      └───────────── 重新上传 ──────────────────┘
```

---

## 3. 技术方案

### 3.1 技术栈

| 层级 | 技术选型 |
|------|----------|
| 前端 | 原生 HTML + CSS + JavaScript |
| 后端 | Cloudflare Workers (Node.js) |
| AI API | Remove.bg |

### 3.2 架构图

```
┌─────────────┐      ┌─────────────────┐      ┌────────────┐
│   浏览器     │ ──→ │ Cloudflare      │ ──→ │ Remove.bg  │
│ (前端页面)   │ ←── │ Workers (后端)   │ ←── │   API      │
└─────────────┘      └─────────────────┘      └────────────┘
```

### 3.3 API 集成

**Remove.bg API**

- 端点：`https://api.remove.bg/v1.0/removebg`
- 认证：API Key (Header: `X-Api-Key`)
- 请求方式：POST multipart/form-data
- 免费额度：50 张/月
- 付费：$0.019/张

---

## 4. 页面设计

### 4.1 页面结构

```
┌──────────────────────────────────────┐
│              Header                  │
│         "Remove Background"          │
├──────────────────────────────────────┤
│                                      │
│         [上传区域 / 拖拽区]           │
│            点击或拖拽上传             │
│                                      │
├──────────────────────────────────────┤
│                                      │
│    [原图预览]        [处理结果预览]   │
│                                      │
├──────────────────────────────────────┤
│         [下载按钮]                   │
└──────────────────────────────────────┘
```

### 4.2 样式要求

- 简洁现代风格
- 透明背景用棋盘格效果展示
- 移动端适配

---

## 5. 约束与限制

| 限制项 | 说明 |
|--------|------|
| 文件大小 | ≤ 10MB |
| 格式支持 | JPG, PNG |
| 并发限制 | 同一时间 1 个请求 |
| 免费额度 | 每月 50 张（Remove.bg 免费版） |
| 存储 | 无，所有图片存于内存/Base64 |

---

## 6. 非功能性需求

- **可用性**：页面加载时间 < 2s
- **兼容性**：Chrome, Edge, Safari, Firefox
- **移动端**：支持手机浏览器

---

## 7. 后续迭代方向 (MVP 后)

- [ ] 批量处理多张图片
- [ ] 自部署 AI 模型（降低 API 成本）
- [ ] 用户登录 / 额度管理
- [ ] 历史记录
- [ ] 导出其他格式（JPG with white background）

---

## 8. 环境变量

| 变量名 | 说明 | 示例 |
|--------|------|------|
| REMOVE_BG_API_KEY | Remove.bg API Key | `xxxxx` |

---

**文档版本**：v1.0  
**创建日期**：2026-03-27
